---
name: "alphanodes.setup.cifs_mount"
'on':
  pull_request:
  push:
    branches:
      - main
    paths:
      - 'roles/cifs_mount/**'
      - 'molecule/cifs_mount/**'
      - '.github/workflows/cifs_mount.yml'

defaults:
  run:
    working-directory: 'cifs_mount'

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      PY_COLORS: 1
      ANSIBLE_FORCE_COLOR: 1

    strategy:
      matrix:
        distro:
          - ubuntu2404
          - debian12
          - debian13

    steps:
      - name: Check out the codebase.
        uses: actions/checkout@v5
        with:
          path: 'cifs_mount'

      - name: Set up Python 3.
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'

      - name: Install test dependencies.
        run: |
          python -m pip install --no-cache-dir --upgrade pip
          python -m pip install -r requirements.txt

      - name: Run Molecule tests.
        run: |
          molecule --version
          molecule test -s cifs_mount
        env:
          MOLECULE_DISTRO: ${{ matrix.distro }}
