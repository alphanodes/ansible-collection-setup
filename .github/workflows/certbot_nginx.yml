---
name: "alphanodes.setup.certbot_nginx"
'on':
  pull_request:
  push:
    branches:
      - main
    paths:
      - 'roles/certbot/**'
      - 'roles/nginx_mono/**'
      - 'molecule/certbot_nginx/**'
      - '.github/workflows/certbot_nginx.yml'

defaults:
  run:
    working-directory: 'certbot_nginx'

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      PY_COLORS: 1
      ANSIBLE_FORCE_COLOR: 1

    strategy:
      fail-fast: false
      matrix:
        distro:
          - ubuntu2404
          - ubuntu2604
          - debian12
          - debian13

    steps:
      - name: Check out the codebase.
        uses: actions/checkout@v5
        with:
          path: 'certbot_nginx'

      - name: Set up Python 3.
        uses: actions/setup-python@v5
        with:
          python-version: '3.13'

      - name: Install test dependencies.
        run: |
          python -m pip install --no-cache-dir --upgrade pip
          python -m pip install -r requirements.txt

      - name: Run Molecule tests.
        run: |
          molecule --version
          molecule test -s certbot_nginx
        env:
          MOLECULE_DISTRO: ${{ matrix.distro }}
