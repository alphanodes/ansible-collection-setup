---
name: "alphanodes.setup.mysql_mariadb"
'on':
  pull_request:
  push:
    branches:
      - main
    paths:
      - 'roles/mysql/**'
      - 'roles/common/**'
      - 'molecule/mysql_mariadb/**'
      - '.github/workflows/mysql_mariadb.yml'

defaults:
  run:
    working-directory: 'mysql_mariadb'

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    env:
      PY_COLORS: 1
      ANSIBLE_FORCE_COLOR: 1

    strategy:
      matrix:
        distro:
          # MariaDB comes from the distribution, so every supported release works
          - ubuntu2404
          - ubuntu2604
          - debian12
          - debian13

    steps:
      - name: Check out the codebase.
        uses: actions/checkout@v5
        with:
          path: 'mysql_mariadb'

      - name: Set up Python 3.
        uses: actions/setup-python@v5
        with:
          python-version: '3.13'

      - name: Install test dependencies.
        run: |
          python -m pip install --no-cache-dir --upgrade pip
          python -m pip install -r requirements.txt

      - name: Run Molecule tests.
        run: |
          molecule --version
          molecule test -s mysql_mariadb
        env:
          MOLECULE_DISTRO: ${{ matrix.distro }}
