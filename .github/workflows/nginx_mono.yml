---
name: "alphanodes.setup.nginx_mono"
'on':
  pull_request:
  push:
    branches:
      - main
    paths:
      - 'roles/nginx_mono/**'
      - 'molecule/nginx_mono/**'
      - '.github/workflows/nginx_mono.yml'

defaults:
  run:
    working-directory: 'nginx_mono'

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      PY_COLORS: 1
      ANSIBLE_FORCE_COLOR: 1
      ANSIBLE_COLLECTIONS_PATHS: ${{ github.workspace }}/nginx_mono
      ANSIBLE_FILTER_PLUGINS: ${{ github.workspace }}/nginx_mono/plugins/filter

    strategy:
      matrix:
        distro:
          - ubuntu2404
          - debian12
          - debian13

    steps:
      - name: Check out the codebase.
        uses: actions/checkout@v5
        with:
          path: 'nginx_mono'

      - name: Set up Python 3.
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'

      - name: Install test dependencies.
        run: |
          python -m pip install --no-cache-dir --upgrade pip
          python -m pip install -r requirements.txt

      - name: Run Molecule tests.
        run: |
          molecule --version
          molecule test -s nginx_mono
        env:
          MOLECULE_DISTRO: ${{ matrix.distro }}
