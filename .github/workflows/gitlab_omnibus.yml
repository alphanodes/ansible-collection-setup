---
name: "alphanodes.setup.gitlab_omnibus"
'on':
  pull_request:
  push:
    branches:
      - main
    paths:
      - 'roles/gitlab_omnibus/**'
      - 'molecule/gitlab_omnibus/**'
      - '.github/workflows/gitlab_omnibus.yml'

defaults:
  run:
    working-directory: 'gitlab_omnibus'

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      PY_COLORS: 1
      ANSIBLE_FORCE_COLOR: 1

    strategy:
      matrix:
        distro:
          - debian12

    steps:
      - name: Check out the codebase.
        uses: actions/checkout@v5
        with:
          path: 'gitlab_omnibus'

      - name: Set up Python 3.
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'

      - name: Install test dependencies.
        run: |
          python -m pip install --no-cache-dir --upgrade pip
          python -m pip install -r requirements.txt

      - name: Run Molecule tests.
        run: |
          molecule --version
          molecule test -s gitlab_omnibus
        env:
          MOLECULE_DISTRO: ${{ matrix.distro }}
