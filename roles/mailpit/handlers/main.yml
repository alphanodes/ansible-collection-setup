---

- name: Reload systemd daemon
  ansible.builtin.systemd:
    daemon_reload: true

- name: Restart mailpit
  ansible.builtin.systemd_service:
    name: mailpit
    state: restarted
  when: mailpit_enabled | bool
