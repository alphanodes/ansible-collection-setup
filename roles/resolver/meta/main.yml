---

dependencies:
  - role: alphanodes.setup.common
    public: true

galaxy_info:
  description: DNS resolver configuration (static resolv.conf or systemd-resolved) with optional local unbound cache
  author: alexandermeindl
  company: AlphaNodes GmbH
  license: Apache License 2.0
  min_ansible_version: '2.18'
  platforms:
    - name: Ubuntu
      versions:
        - noble
        - resolute
    - name: Debian
      versions:
        - bullseye
        - bookworm
        - trixie
  galaxy_tags:
    - dns
    - resolver
    - systemd
