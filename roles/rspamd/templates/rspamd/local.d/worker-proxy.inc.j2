# Rspamd proxy worker - Milter protocol for Postfix integration
# see https://docs.rspamd.com/workers/rspamd_proxy

# Enable milter protocol
milter = yes;

# I/O timeout for milter connections
timeout = {{ rspamd_proxy_timeout | default('120s') }};

# Upstream configuration
upstream "local" {
  # Use this upstream by default
  default = yes;

  # Scan on the same host (self_scan mode)
  self_scan = yes;
}

# Bind to localhost only (security)
bind_socket = "localhost:11332";

# Maximum number of retries for upstream connections
max_retries = {{ rspamd_proxy_max_retries | default(5) }};
