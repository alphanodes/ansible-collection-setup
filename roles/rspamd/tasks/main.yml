---

- name: Include setup tasks
  ansible.builtin.include_tasks: setup.yml
  tags:
    - rspamd
  when: not rspamd_remove

- name: Include remove tasks
  ansible.builtin.include_tasks: remove.yml
  tags:
    - rspamd
    - remove
  when: rspamd_remove
