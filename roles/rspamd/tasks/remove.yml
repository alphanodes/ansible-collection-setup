---

- name: Remove rspamd package
  ansible.builtin.apt:
    name: rspamd
    purge: true
    state: absent

- name: Remove configuration
  ansible.builtin.file:
    name: '{{ item }}'
    state: absent
  loop:
    - /etc/rspamd
    - /var/lib/rspamd
