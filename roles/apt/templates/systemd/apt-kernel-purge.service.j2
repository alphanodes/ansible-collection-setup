{{ managed_by_ansible | comment }}

[Unit]
Description=Purge old unused kernel packages
After=network.target

[Service]
Type=oneshot
ExecStart=/bin/bash -c 'dpkg --get-selections "linux-*" | grep deinstall | awk "{print $$1}" | xargs --no-run-if-empty dpkg --purge'
