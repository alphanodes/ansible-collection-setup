---
# Variables that cannot be overridden are defined here
# Note: mysql_real_config_file and mysql_config_include_file moved to defaults/main.yml
# to allow MariaDB compatibility for local ARM64 testing
